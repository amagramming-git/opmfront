import type { NextPage } from "next";
import { Container, Row } from "react-bootstrap";

const Home: NextPage = () => {
	return (
		<Container>
			<Row>
				<h1>Home</h1>
			</Row>
		</Container>
	);
};

export default Home;
